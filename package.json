{
  "name": "meal-prep",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "vp run -r build",
    "dev": "vp run -r dev",
    "lint": "vp run -r lint",
    "format": "vp run -r format",
    "typecheck": "vp run -r typecheck",
    "prepare": "vp config"
  },
  "devDependencies": {
    "typescript": "5.9.3",
    "vite-plus": "catalog:"
  },
  "packageManager": "pnpm@10.32.1",
  "engines": {
    "node": ">=20"
  }
}
